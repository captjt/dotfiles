- clean: ['~']

- link:
    ~/.zshrc: config/zshrc

    ~/.bin:
      path: bin
      create: true

    # Git:
    ~/.gitconfig: config/gitconfig

    # Editors:
    ~/.editorconfig: config/editorconfig
